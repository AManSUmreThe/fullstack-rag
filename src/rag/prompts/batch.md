Rank the movies listed below by relevance to the following search query.

Query: "{query}"

Movies:
{doc_list_str}

ALL movies should be ranked. Return ONLY the movie IDs in order of relevance (best match first).ALWAYS Return a valid JSON list, nothing else.

For example:
[75, 12, 34, 2, 1]

Ranking: